# Leave DATABASE_URL unset to run locally on PGlite with no setup at all.
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Salts the daily hash of each visitor's address. Any long random string.
HASH_SALT=

# The one key to /admin. Any long random string. Leave empty to disable admin entirely.
ADMIN_KEY=
