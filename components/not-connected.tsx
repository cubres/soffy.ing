// Shown in place of the feed while the site has no database behind it.
export default function NotConnected() {
  return <p className="muted">the site is not connected to its database yet. the page still works: <a href="/">write</a>.</p>
}
